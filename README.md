# Weather ETL Pipeline

This repository contains a PySpark weather ETL pipeline.

- `weather-transform-pipeline.ipynb`: transform notebook
- `extract/fetch_weather.py`: extraction helper
- `data/`: local JSON weather files

Run the notebook after placing your JSON files inside the `data/` folder.
